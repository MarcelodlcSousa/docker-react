sudo: required
language: generic
services:
  - docker

before_install:
  - docker build -t marcelaao/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true marcelaao/docker-react npm run test
